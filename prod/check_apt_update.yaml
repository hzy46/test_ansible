---
- hosts: localhost
  tasks:
  - name: check apt update
    action: apt
    args:
      update_cache: true
    register: check_apt_update
    retries: 3
    until: check_apt_update is succeeded
    ignore_errors: true
  - fail:
      msg: 'Please check your `sudo apt update` status, make sure it has a non-zero exits'
    when: check_apt_update is failed
